    # Application Settings
    ENVIRONMENT=development
    DEBUG=False

    # Database Settings
    # Example: sqlite:///./data/transcript_engine.db
    DATABASE_URL=sqlite:///./data/transcript_engine.db

    # Vector Store Settings
    VECTOR_STORE_PATH=./data/vector_store
    # CHROMA_COLLECTION=transcripts # Optional: Add if needed later

    # LLM Settings
    OLLAMA_BASE_URL=http://host.docker.internal:11434 # Use host.docker.internal for host access from container
    DEFAULT_MODEL=llama3 # Or mistral, llama2, etc.

    # Embedding Settings
    EMBEDDING_MODEL=BAAI/bge-small-en-v1.5

    # External Transcript API (Placeholder - Replace with actual values)
    TRANSCRIPT_API_URL=http://example.com/api/transcripts
    TRANSCRIPT_API_KEY=
    LIMITLESS_API_KEY=

    # OpenAI API Key (Optional - For potential fallback or other features)
    # OPENAI_API_KEY=